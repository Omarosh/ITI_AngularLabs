import { ArraySplicePipe } from './array-splice.pipe';

describe('ArraySplicePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySplicePipe();
    expect(pipe).toBeTruthy();
  });
});
